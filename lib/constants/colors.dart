part of animated_segment;

class AppColors {
  static const primary = Color(0xff0217FD);
  static const bgColor = Color(0xff8AADFB);
  static const white = Colors.white;
  static const transparent = Colors.transparent;
}
